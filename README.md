# Multimodal PDF RAG

Q&A system with a pdf that contains text, images, and tables

requires .env with ANTHROPIC_API_KEY

```bash
python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt

python3 pdf_qa_system.py
```